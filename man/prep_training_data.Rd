% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prep_training_data.R
\name{prep_training_data}
\alias{prep_training_data}
\title{Align training data and apply a method of elastic fPCA}
\usage{
prep_training_data(
  f,
  time,
  fpca_method,
  ci = c(-2, -1, 0, 1, 2),
  optim_method = "DP"
)
}
\arguments{
\item{f}{Training data matrix (M x N) of N functions with M samples (already smoothed)}

\item{time}{Vector of size M describing the sample points}

\item{fpca_method}{Character string specifying the type of elastic fPCA method to use ('jfpca', 'hfpca', or 'vfpca')}

\item{ci}{Geodesic standard deviations to be computed (default = c(-2, -1, 0, 1, 2))}

\item{optim_method}{Method used for optimization when computing the Karcher mean (DP,DP2,RBFGS,DPo)}
}
\value{
List with three objects:
\itemize{
  \item alignment: output from fdasrvf::time_warping
  \item fpca_type: type of elastic FPCA method applied
  \item fpca_res: output from fdasrvf::jointFPCA, fdasrvf::horizFPCA, or fdasrvf::vertFPCA (dependent on fpca_type)
}
}
\description{
Applies steps 2 and 3 of the VEESA pipeline (alignment and elastic fPCA) to the training data
    in preparation for inputting the data to the model in step 4.
}
